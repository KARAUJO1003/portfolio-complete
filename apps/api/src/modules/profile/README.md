# Profile Module

Singleton por usuario para dados principais do portfolio/curriculo.

Rotas:

- `GET /profile/me`
- `PUT /profile/me`

O modulo usa `ownerId` para manter multiusuario possivel desde o MVP.
