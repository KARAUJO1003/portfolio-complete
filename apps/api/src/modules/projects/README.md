# Projects Module

CRUD de projetos usados no portfolio e no curriculo.

Rotas privadas:

- `GET /projects`
- `POST /projects`
- `PUT /projects/:id`
- `DELETE /projects/:id`

Campos de visibilidade indicam se o projeto pode aparecer no portfolio, no curriculo ou em ambos.
